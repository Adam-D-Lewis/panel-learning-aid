name: panel
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - pandas
  - panel =0.9.7
  - holoviews
  - hvplot
  - bokeh =2.2
  - jupyterlab
  - pytest
  - appmode
  - pip
# pip
  - pip:
      - mortgage
# install @pyviz/jupyterlab_pyviz jupyter labextension afterwards as well if using jupyterlab
